import styles from "./IconNew.module.scss";

const IconNew = function () {
  return (
    <figure className={styles.figure}>
      <span className={styles.text}>New</span>
    </figure>
  );
};

export default IconNew;
